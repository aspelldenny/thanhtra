name: Validate Thanh Tra

on:
  push:
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate skill structure
        run: ./scripts/validate-skill.sh

      - name: Validate regression fixtures
        run: ./scripts/validate-fixtures.sh
